---
"@okkly/angular": minor
---

Add `OkklyRating` (`okkly-rating`), a star or heart rating with half steps, hover preview, arrow-key stepping, a read-only display and an `okklyRatingIcon` template for custom glyphs.

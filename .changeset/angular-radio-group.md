---
"@okkly/angular": minor
---

Add `OkklyRadioGroup` (`okkly-radio-group`), mirroring `@okkly/react`'s `<RadioGroup>`: nested `okkly-radio` options share a name and the group's `[(value)]`, and inherit its `disabled`, `size` and `color` unless they set their own.

---
"@okkly/angular": minor
---

Add `OkklyTimePicker` (`okkly-time-picker`), an always-visible inline time picker with scrollable hour/minute/AM-PM columns, native scroll-snap momentum, `step`, `format`, and accent `color`.

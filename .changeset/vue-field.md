---
"@okkly/vue": minor
---

Add the internal `Field` shell (label, control box, adornments, helper text), mirroring `@okkly/react`'s `<Field>`. Not exported — it is the wrapper `TextField` renders inside.

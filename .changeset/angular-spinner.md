---
"@okkly/angular": minor
---

Add `OkklySpinner` (`okkly-spinner`), an indeterminate loading ring mirroring `@okkly/react`'s `<Spinner>`: three size presets, nine tones, and a `thickness` override.

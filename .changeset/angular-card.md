---
"@okkly/angular": minor
---

Add `OkklyCard` (`okkly-card`) and its slots — `okkly-card-header` (with `okklyCardAvatar`/`okklyCardAction`), `okkly-card-content`, `okkly-card-actions` and `img[okklyCardMedia]` — mirroring `@okkly/react`'s `<Card>` family: five surfaces, four padding presets, an aura tone and an interactive hover lift.

---
"@okkly/vue": minor
"@okkly/vue-composables": minor
---

Add `Select`, a closed list of options in a field, mirroring `@okkly/react`'s `Select`. `@okkly/vue-composables` gains the headless `useSelect` composable it's built on, the Vue port of `@okkly/react-hooks`'s `useSelect`.

---
"@okkly/vue": minor
---

Add `Drawer`, a panel that slides in from an edge and takes the page with it — temporary (built on `Modal`), persistent, or permanent — mirroring `@okkly/react`'s `Drawer`.

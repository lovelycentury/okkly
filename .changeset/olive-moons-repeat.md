---
"@okkly/svelte": minor
---

Add `@okkly/svelte`, the Svelte 5 build of the design system, with `<Button>` (variant/color/shape/size/loading/ripple parity with `@okkly/react`, icons as snippets) and the `ripple` action.

---
"@okkly/angular": minor
---

Add `OkklyBadge` (`okkly-badge`), a count or status dot pinned to projected content, or a standalone pill without it, mirroring `@okkly/react`'s `<Badge>`: nine tones, dot variant, `max` overflow, `invisible`, `overlap` and `anchorOrigin`.

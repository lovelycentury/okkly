---
"@okkly/angular": minor
---

Add `OkklyEmptyState` (`okkly-empty-state`), the panel that stands in for an empty list, with a severity icon or a projected `okklyEmptyStateIcon` glyph and an `okklyEmptyStateAction` row.

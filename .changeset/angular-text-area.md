---
"@okkly/angular": minor
---

Add `OkklyTextArea` (`okkly-text-area`), a multi-line text field with a two-way `value`, a character counter for `maxLength` and `autosize` between `rows` and `maxRows`.

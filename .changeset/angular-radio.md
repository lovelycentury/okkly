---
"@okkly/angular": minor
---

Add `OkklyRadio` (`okkly-radio`), one option of a single choice mirroring `@okkly/react`'s `<Radio>`: `[(checked)]`, a built-in label, three sizes and six tones, with name, value, id and aria-label on the native input.

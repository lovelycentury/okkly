---
"@okkly/angular": minor
---

Add `OkklyIconButton` (`button[okklyIconButton]`, `a[okklyIconButton]`), an icon-only control mirroring `@okkly/react`'s `<IconButton>`: ghost, glass or solid, six tones, three sizes, the press ripple, and a disabled state that also covers links.

---
"@okkly/angular": minor
---

Add `OkklyAutocomplete` (`okkly-autocomplete`), a filter-as-you-type field with single and multi-select tags, free text (`freeSolo`), grouping, clearing, loading and a native form fallback via `name`. Built on the same `OkklyOption*` primitives and `SelectOption` shape as `OkklySelect`.

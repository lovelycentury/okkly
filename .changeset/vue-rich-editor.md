---
"@okkly/vue": minor
---

Add `RichEditor`, a TipTap-based rich text editor with toolbar, slash menu, word count and autosave status, mirroring `@okkly/react`'s `RichEditor`. Built on `@tiptap/vue-3` instead of `@tiptap/react`.

---
"@okkly/angular": minor
---

Add `OkklyDateTimePicker` (`okkly-date-time-picker`), a fixed inline card combining a `Calendar` and a `TimePicker` with a selected-time summary and a Confirm button.

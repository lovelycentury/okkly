---
"@okkly/vue": minor
---

Add `Dialog`, along with the `DialogTitle`, `DialogContent`, `DialogActions` and `DialogClose` subcomponents, mirroring `@okkly/react`'s `Dialog`. It builds on `Modal` for the portal, backdrop, focus trap and scroll lock, and opens the paper with a `Grow` transition.

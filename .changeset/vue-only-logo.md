---
"@okkly/vue": minor
"@okkly/vue-composables": minor
---

Add `Only`, a viewport gate that mounts its default slot only within a breakpoint range, backed by a new `useMediaQuery` composable, and `Logo`, the static brand lockup — both mirroring `@okkly/react`'s equivalents.

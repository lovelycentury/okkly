---
"@okkly/angular": minor
---

Add `OkklyChipGroup` (`okkly-chip-group`) with the `okklyChipGroupOption` chip marker, mirroring `@okkly/react`'s `<ChipGroup>`: single (`exclusive`) or multiple selection bound with `[(value)]`, six tones for selected chips, and a `disabled` state.

---
"@okkly/angular": minor
---

Add `OkklyOnly` (`*okklyOnly="{ from, to }"`), a structural directive that creates its element only while the viewport is within a breakpoint range, and the `mediaQuery` helper behind it.

---
"@okkly/angular": minor
---

Add `OkklyTypography` (`[okklyTypography]`), the editorial type scale as a directive: `variant`, `color`, `align`, `gutterBottom` and `noWrap` mirror `@okkly/react`'s `<Typography>` name-for-name, applied to whatever element the markup calls for.

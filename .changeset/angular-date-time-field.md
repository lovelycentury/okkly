---
"@okkly/angular": minor
---

Add `OkklyDateTimeField` (`okkly-date-time-field`), a masked `dd.mm.yyyy, HH:mm` date+time input with a `DateTimePicker` popover, min/max bounds, and an accent tone via `color`.

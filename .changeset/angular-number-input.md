---
"@okkly/angular": minor
---

Add `OkklyNumberInput` (`okkly-number-input`), a numeric field with a two-way `number | null` value, stepper or chevron controls, arrow-key stepping and clamping to `min`/`max`.

---
"@okkly/angular": minor
---

Add `OkklySwitch` (`okkly-switch`), an immediate on/off toggle mirroring `@okkly/react`'s `<Switch>`: a native checkbox with `role="switch"`, `[(checked)]`, a built-in label, three sizes and six tones.

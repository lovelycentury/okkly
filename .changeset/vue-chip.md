---
"@okkly/vue": minor
---

Add `Chip` and `ChipGroup`, mirroring `@okkly/react`'s components of the same names. `ChipGroup` composes `Chip`s into a wrapping row with single- or multi-select, driven by an `items` array and `v-model`.

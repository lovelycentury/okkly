---
"@okkly/angular": minor
---

Add `OkklyAvatar` (`okkly-avatar`), a person's image or initials mirroring `@okkly/react`'s `<Avatar>`: falls back to the initials when the image fails, with a presence dot, two shapes, three sizes and three tones.

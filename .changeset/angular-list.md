---
"@okkly/angular": minor
---

Add `OkklyList` (`ul[okklyList]`) and `OkklyListItem` (`li[okklyListItem]`) with `okkly-list-item-text`, `okkly-list-item-icon` and the `okklyListItemStart`/`okklyListItemEnd` slots; a `button` row renders a real `<button>`.

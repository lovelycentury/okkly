---
"@okkly/angular": minor
---

Add `OkklyCheckboxGroup` (`okkly-checkbox-group`), mirroring `@okkly/react`'s `<CheckboxGroup>`: nested `okkly-checkbox` options share a name and the group's `[(value)]` array, and inherit its `disabled`, `size` and `color` unless they set their own.

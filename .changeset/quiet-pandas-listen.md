---
"@okkly/vue": minor
---

Add `@okkly/vue`, the Vue build of the design system, with `<Button>` (variant/color/shape/size/loading/ripple parity with `@okkly/react`, icons as slots) and the `useRipple` composable behind `<Ripple>`.

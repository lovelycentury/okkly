---
"@okkly/angular": minor
---

Add `OkklyCheckbox` (`okkly-checkbox`), a binary or indeterminate choice mirroring `@okkly/react`'s `<Checkbox>`: `[(checked)]` and `[(indeterminate)]`, a built-in label, three sizes and nine tones.
